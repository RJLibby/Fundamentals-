/*
* Variables, Data Types, and Typing
 */
let wordData;
wordData = "Word Data"

console.log(wordData);

let subTotal = 100;
let tax = subTotal * 0.07;
let totalWithTax = tax + subTotal;


let obj ={
    key1: "value",
    key2: 4,
    boolean: true,
    obj2: {
        obj2key: "value of inner object",
    }
};

console. log(obj.obj2.obj2key1);


